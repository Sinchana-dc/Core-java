class UnionBank {
public static boolean createAccount(String fullName,String dob,String email,long phNumber,
-String panNumber,long aadharNumber,String gender);
boolean isAccountCreated =false;
		 if(cprLoc!=null && dcdrLocation>0 && givenName!=null && dob!=null && email!=null && surName!=null
		 && isemailLoginsame!=null && loginId!=null && pw!=null && hide!=null && confirmPw!=null && capt!=null){
		 cprLoc =cprLocation;
		 dcdrLoc =dcdrLocation;
		 givenNm =givenName;
		 db =dob;
		 eml =email;
		 surNm =surName;
		 login=isEmailLoginSame;
		 logId= logInId;
		 pw= pwd;
		 hide=hideAns;
		 confirmPw =confirmPwd;
		 capt=captcha;
		 }